{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "make clean;make",
            "args": [
                // "-g",
                // "${file}",
                // "-o",
                // "${workspaceFolder}"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}